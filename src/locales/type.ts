type Messages = {
  [key: string]: {
    amount: string;
    calculate: string;
    "rent&utils": string;
    food: string;
    entertainment: string;
    total: string;
    remains: string;
  };
};

export default Messages;
